# Stage 16 C-C-D-V Prompt — 2026-05-05

## Context

MindMosaic v1 Stage 16 — Phase 1 Day 19. Stage 15 closed (commit `a8c3076` evening close). 208 → 257 unit tests, 7 packages, 22 → 23 ADRs. `@mm/engines` had `contracts.ts` + `linear.ts` + 28 tests; `EngineState` aliased `LinearEngineState`. Stage 16 introduces SkillEngine (Spec §3.2.3) + DiagnosticEngine (Spec §3.2.4) per DEV_PLAN.md L188–193.

The §2A pre-implementation review (run earlier in this session, archived in this file's predecessor) produced 13 binding decisions (Q-16.1 through Q-16.13). All defaults accepted as proposed.

Prerequisite state:
- HEAD: `a8c3076` (Stage 15 evening close)
- `EngineState = LinearEngineState` aliased
- `TerminationReason` had 4 values
- `EngineResponse` had no telemetry field
- No `EngineItem` type — items consumed as `ItemDTO`
- No shared test fixtures — Stage 15's `linear.test.ts` had local helpers

## Constraints

**From Q-16.1 through Q-16.13 (binding):**

- Q-16.1: Widen `EngineState` to `z.discriminatedUnion('engine_type', [Linear, Skill, Diagnostic])`.
- Q-16.2: Extend `TerminationReason` with `mastery_reached`, `max_items_reached`, `confidence_threshold_met`.
- Q-16.3: `mastery_threshold` default 0.85, configurable per `FrameworkConfig`.
- Q-16.4: `EngineResponse.telemetry?: { time_to_answer_ms, answer_changes }` (optional).
- Q-16.5: `EngineItem extends ItemDTO` server-side; LinearEngine `planned_items` migrates to `EngineItem[]`. Wire `ItemDTO` stays lean.
- Q-16.6: `score()` preserved on `AssessmentEngine` interface; per-engine helpers for real outputs (`masteryDelta()`, `proficiencyMap()`).
- Q-16.7: `confidence_threshold` default 0.7.
- Q-16.8: `max_items` default 20.
- Q-16.9: Tie-break lexicographic by `skill_id` (then `item_id`).
- Q-16.10: `diagnostic_start_difficulty` default 0.5.
- Q-16.11: Single commit per BUILD_CONTRACT §11.1.
- Q-16.12: Test fixtures into shared `_fixtures.ts`.
- Q-16.13: File ADR-0023 documenting state union + EngineItem.

**Cumulative engine disciplines (ADR-0022 + Stage 16 additions):**

- Pure-function namespaces only.
- `clock: () => number` injected per-call to `getTimeRemaining` + `terminate`; never stored in state.
- No `Math.random`, no `Date.now()` inside engine bodies.
- All `EngineState` JSON-serialisable.
- Each engine method body starts with `assert{X}State(state)` for narrowing.

**Repo-wide:**

- TypeScript strict; `noPropertyAccessFromIndexSignature`, `noUncheckedIndexedAccess`, `noImplicitOverride`.
- BUILD_CONTRACT §11.2: no AI Co-Authored-By trailers.
- BUILD_CONTRACT §11.1: one stage = one atomic commit.

## Deliverables

1. **`packages/engines/src/contracts.ts`** — extend with `EngineItem`, telemetry on `EngineResponse`, three new `TerminationReason` values, `SkillEngineState` + `DiagnosticEngineState` schemas + types, `EngineState` widened to `z.discriminatedUnion`, `MasteryDeltaResult` + `ProficiencyResult` schemas, Stage 16 fields on `FrameworkConfig`, `assert{Linear,Skill,Diagnostic}State` helpers. `LinearEngineState.planned_items` migrates to `EngineItem[]`.
2. **`packages/engines/src/skill.ts`** — `SkillEngine: AssessmentEngine` namespace + helpers `cognitiveLoad`, `prioritiseSkills`, `masteryDelta`. Implements §7.5.1 (difficulty rule), §7.5.2 (skill prioritisation), §9.5 (cognitive load).
3. **`packages/engines/src/diagnostic.ts`** — `DiagnosticEngine: AssessmentEngine` namespace + helpers `proficiencyMap`, `estimateConfidence`. Binary-search-over-difficulty; v1 confidence model = structural narrowing.
4. **`packages/engines/src/index.ts`** — barrel re-export `skill.js` + `diagnostic.js`.
5. **`packages/engines/src/__tests__/_fixtures.ts`** — shared deterministic builders for items, pools, responses, sessions, configs.
6. **`packages/engines/src/__tests__/skill.test.ts`** — ~22 tests including the two named DEV_PLAN exit criteria: cognitive load >0.8 reduces difficulty, mastery threshold terminates.
7. **`packages/engines/src/__tests__/diagnostic.test.ts`** — ~18 tests including binary-search convergence (12-item probe → range ≤ 0.1).
8. **`packages/engines/src/__tests__/linear.test.ts`** — refactor to consume `_fixtures.ts`; 28/28 still pass.
9. **`packages/engines/src/linear.ts`** — refactor to use `assertLinearState` narrowing under the new union (no behavioural change). `score().skills_touched` aggregates real `skill_ids` from answered items (Stage 15 documented gap closed).
10. **`docs/dev/decisions/0023-engine-state-union-and-engine-item.md`** — ADR-0023.

## Verification

- `pnpm install` → 0 errors.
- `pnpm typecheck` → 0 errors across all 7 packages.
- `pnpm lint` → 0 warnings or errors across all 7 packages.
- `pnpm test` → 257/257 pass:
  - `@mm/types`: 97 (1 file).
  - `@mm/sdk`: 24 (3 files).
  - `@mm/ui`: 59 (27 files).
  - `@mm/engines`: 77 (3 files: 28 linear + 27 skill + 22 diagnostic).
- `pnpm build` → 7/7 packages green; `apps/web` First Load JS unchanged at 87.3 kB shared (engines smoke is type-only, erased).
- Pre-commit husky hook ran turbo typecheck + lint (14 tasks) before accepting commit.
- DEV_PLAN exit criteria explicitly named in test descriptions:
  - `'cognitive_load > 0.8 with moderate accuracy → difficulty − cognitive_load_step_down (-0.1) — DEV_PLAN exit criterion'`.
  - `'mastery threshold terminates — DEV_PLAN exit criterion'`.
- `git push origin main` succeeded (range `a8c3076..496a659`).
- Working tree clean except for `apps/web/.env.local.example` (pre-existing local edit, intentionally excluded).

## Outcome

- **Commit:** `496a659` on `main`, pushed to `origin/main`.
- **Tests added:** 49 (27 skill + 22 diagnostic; 28 linear unchanged).
- **New ADR:** ADR-0023 (engine state union + EngineItem).
- **Stage 15 documented gap closed:** `LinearEngine.score().skills_touched` now emits real values via `EngineItem.skill_ids` plumbing.
- **Forward-compatible state union ready** for Stage 17's `AdaptiveEngineState` (fourth branch) and v1.1's `RepairEngineState` (fifth branch) — drop-in additions, no other code changes needed.
