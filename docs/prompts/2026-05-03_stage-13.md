# Stage 13 C-C-D-V Prompt — 2026-05-03

## Context

Stage 13 follows completion of packages/types (Stage 11) and packages/sdk (Stage 12). The monorepo
has working TypeScript strict packages with NodeNext module resolution. packages/ui/src/index.ts
and packages/ui/package.json were previously empty/minimal skeletons. The Supabase backend and DB
migrations are complete through Stage 10. The unit test suite stands at 121 tests.

Tech stack (locked): Turborepo + pnpm 9; Next.js 14 App Router; TypeScript strict; Tailwind +
Radix UI primitives (ADR-0020: shadcn/ui wording means "Radix-based"); Vitest + jest-axe.

Execution directives approved before coding:
- X1: Token import discipline — tokens.css imported once at app shell; components use CSS vars via
  tailwind.preset.ts; no inline style={} props.
- X2: axe CI threshold — toHaveNoSeriousViolations (fail serious/critical; warn moderate/minor via
  console.info).
- X3: forwardRef on all Radix wrappers (Dialog.Content, Tooltip.Trigger, Select.Trigger).
- X4: UI-DIVERGENCE — axe gate moves from storybook:test (BUILD_CONTRACT §10) to pnpm test (Vitest).
  BUILD_CONTRACT correction deferred to Stage 14 audit.
- X5: Storybook + @storybook/addon-a11y for dev-time visual review; NOT the CI gate.
- X6: Button/IconButton size="md" minimum 44px height (h-11 Tailwind class); asserted via className.

## Constraints

- NodeNext module resolution: all relative imports need .js extensions.
- packages/ui has "type": "module" in package.json (ESM).
- No shadcn CLI — Radix UI headless primitives directly.
- Strict TypeScript: no `any` in production code.
- jest-axe@9 ships no TypeScript declarations — needs ambient module declaration.
- @testing-library/jest-dom type augmentation for Vitest must be declared directly in types.d.ts
  (import from setup.ts does not propagate globally under NodeNext tsconfig).
- afterEach(cleanup) must be explicit in Vitest setup (auto-cleanup unreliable with jsdom).
- Radix Checkbox/RadioGroup/Select: sibling `<label for>` not computed by axe-core in jsdom.
  Fix: add aria-label directly to Radix root/item/trigger elements.

## Deliverables

Phase A — Foundation:
- packages/ui/src/tokens.css (UI_CONTRACT §2 CSS custom properties)
- packages/ui/src/tailwind.preset.ts (Tailwind theme extension)
- packages/ui/tailwind.config.ts + postcss.config.js
- packages/ui/tsconfig.json (lib + jsx additions)
- packages/ui/package.json (Radix deps, jest-axe, @testing-library/react, Storybook)
- packages/ui/vitest.config.ts
- packages/ui/src/__tests__/setup.ts (X2 custom matcher + afterEach cleanup)
- packages/ui/src/__tests__/jest-axe.d.ts (ambient module declaration)
- packages/ui/src/__tests__/types.d.ts (vitest Assertion augmentation)
- packages/ui/.storybook/main.ts + preview.ts
- packages/ui/README.md (X5 two-layer a11y docs)
- docs/dev/decisions/0020-radix-not-shadcn.md (ADR-0020)
- CLAUDE.md tech stack update

Phase B — 26 Primitives (each: .tsx + .stories.tsx + .test.tsx):
AppShell, Sidebar, TopBar, PageHeader, EmptyState, ErrorBoundary, LoadingState, NavLink, Tabs,
Breadcrumbs, Card, StatTile, ProgressBar, SkillBar, Table, Button, IconButton, Input, Select,
Checkbox, RadioGroup, TextArea, FormField, Dialog, Toast, Tooltip

Phase C — Wiring:
- packages/ui/src/index.ts (barrel with .js extensions)
- docs/dev/QUESTIONS.md (Q-0001 resolved)

## Verification

- pnpm -C packages/ui typecheck → 0 errors ✅
- pnpm lint (all 7 packages) → 0 errors ✅
- pnpm -C packages/ui test → 50/50 tests passing, 26/26 files ✅
- Commit: d2be303
