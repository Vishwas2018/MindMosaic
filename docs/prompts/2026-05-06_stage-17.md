# Stage 17 C-C-D-V Prompt — 2026-05-06

## Context

MindMosaic v1 Stage 17 — Phase 1, Days 20–21 (compressed to a single session).
Stage 16 closed (commit `1d9f900` evening close). 257 → 290 unit tests, 7
packages, 23 → 24 ADRs. Three engines complete (Linear, Skill, Diagnostic);
`EngineState` is a 3-arm `z.discriminatedUnion` with a clean slot for
`AdaptiveEngineState`. Stage 17 ships AdaptiveEngine for NAPLAN testlet
routing per Spec §3.2.1 + §4.1 — closing the v1 engine quartet.

The §2A pre-implementation review (run in this session's morning ritual)
discovered a **load-bearing spec/seed mismatch (Q-17.1)**: the NAPLAN row's
`adaptive_rules` JSON in `supabase/seeds/03_assessment_config.sql` was
IRT/CAT-shaped (`theta_init`, `step_size`, `max_info`) while spec §3.2.1
explicitly describes testlet routing tables. DEV_PLAN.md Stage 17's
medium-risk note ("Routing table JSON structure must match seed exactly")
implicitly recognised this risk. Resolution chosen: rewrite the seed to a
testlet routing table; document via ADR-0024.

The §2A review also produced 12 binding decisions (Q-17.1 through Q-17.12).
All defaults accepted as proposed.

Prerequisite state:
- HEAD: `1d9f900` (Stage 16 evening close).
- `EngineState = z.discriminatedUnion('engine_type', [Linear, Skill, Diagnostic])`.
- `EngineItem` has `skill_ids` + `difficulty` + optional `discrimination`.
- `EngineResponse.is_correct` is non-nullable `boolean`.
- `TerminationReason` has 7 values (incl. Stage 16 additions).
- No `testlet` table; no `testlet_id` column on `item`.
- Seed NAPLAN `adaptive_rules` is IRT-shaped placeholder.

## Constraints

**From Q-17.1 through Q-17.12 (binding):**

- Q-17.1: (A) Rewrite seed `adaptive_rules` to a testlet routing table.
  Document via ADR-0024.
- Q-17.2: Testlet membership in `adaptive_rules.testlets[]` map +
  optional `EngineItem.testlet_id`. No new migration table.
- Q-17.3: Per-stage timer in `state.stages[i].time_limit_ms` + `started_at`.
- Q-17.4: Reuse `timer_expired` for stage-timer expiry (no new reason).
- Q-17.5: Writing items: `EngineResponse.is_correct: boolean | null`,
  excluded from routing score, `response_data.text` preserved verbatim.
- Q-17.6: Hard-blocked back-nav across stages.
- Q-17.7: `scoreAdaptiveWithConfig` parallel to Linear's
  `scoreWithConfig` (engine-prefixed to avoid barrel collision).
- Q-17.8: Optional `EngineItem.testlet_id`, `stage_id`, `is_writing_item`.
- Q-17.9: Throw `Error` on ambiguous routing-table lookup.
- Q-17.10: Single commit (seed + engine + ADR + tests).
- Q-17.11: File ADR-0024 for the routing-table data model.
- Q-17.12: No skip — proceed now (resolution complete).

**Cumulative engine disciplines (ADR-0022 + ADR-0023 + Stage 17 additions):**

- Pure-function namespaces only.
- `clock: () => number` injected per-call to `getTimeRemaining` + `terminate`;
  never stored in state.
- No `Math.random`, no `Date.now()` inside engine bodies.
- All `EngineState` JSON-serialisable.
- Each engine method body starts with `assert{X}State(state)`.
- AdaptiveEngine: `getNextItem` is a pure peek (no state mutation);
  `recordResponse` does the routing-transition mutation.

**Repo-wide:**

- TypeScript strict; `noPropertyAccessFromIndexSignature`,
  `noUncheckedIndexedAccess`, `noImplicitOverride`.
- BUILD_CONTRACT §11.2: no AI Co-Authored-By trailers.
- BUILD_CONTRACT §11.1: one stage = one atomic commit.

## Deliverables

1. **`supabase/seeds/03_assessment_config.sql`** — rewrite NAPLAN row's
   `adaptive_rules` to testlet routing table (7 testlets, 6 routing rows,
   3-stage NAPLAN Y5 Numeracy). `scoring_rules` updated to placeholder
   `adaptive_path` shape.
2. **`packages/engines/src/contracts.ts`** — extend with
   `RoutingTableEntrySchema`, `TestletDefinitionSchema`, `AdaptiveRulesSchema`,
   `AdaptiveStageStateSchema`, `RoutingHistoryEntrySchema`,
   `AdaptiveEngineStateSchema`. Widen `EngineStateSchema` to 4 arms
   (linear | skill | diagnostic | adaptive). Add optional `testlet_id`,
   `stage_id`, `is_writing_item` to `EngineItem`. Make `EngineResponse.is_correct`
   nullable. Add `FrameworkConfig.adaptive_rules?: AdaptiveRules`. Add
   `assertAdaptiveState`.
3. **`packages/engines/src/adaptive.ts`** — `AdaptiveEngine: AssessmentEngine`
   pure-function namespace. `getNextItem` peeks; `recordResponse` mutates with
   routing transition. Helpers: `computeStageScore`, `lookupRoute`,
   `scoreAdaptiveWithConfig`, `terminateAdaptiveWithConfig`.
4. **`packages/engines/src/index.ts`** — barrel re-export `adaptive.js`.
5. **`packages/engines/src/__tests__/_fixtures.ts`** — `buildAdaptiveRules`,
   `buildAdaptiveItemPool`, `buildAdaptiveSession`, `buildAdaptiveConfig`,
   `buildTestletItems`, `buildWritingItem`. Update `buildResponse` to accept
   `isCorrect: boolean | null` + optional `responseData` override.
6. **`packages/engines/src/__tests__/adaptive.test.ts`** — ~25 tests
   including the DEV_PLAN exit criterion (3-stage golden walkthrough).
7. **`docs/dev/decisions/0024-adaptive-testlet-routing-model.md`** — ADR-0024.

## Verification

- `pnpm install` → 0 errors.
- `pnpm typecheck` → 0 errors across all 7 packages.
- `pnpm lint` → 0 warnings or errors across all 7 packages.
- `pnpm test` → 290/290 pass:
  - `@mm/types`: 97 (1 file).
  - `@mm/sdk`: 24 (3 files).
  - `@mm/ui`: 59 (27 files).
  - `@mm/engines`: 110 (4 files: 28 linear + 27 skill + 22 diagnostic + 33 adaptive).
- `pnpm build` → 7/7 packages green; `apps/web` First Load JS unchanged.
- Pre-commit husky hook ran turbo typecheck + lint (14 tasks) before
  accepting commit.
- DEV_PLAN exit criterion explicitly named in test description:
  `'AdaptiveEngine — golden 3-stage NAPLAN session (DEV_PLAN exit criterion)'`
  asserts: routing_history `[s1,3→t2_medium], [s2,4→t3_hard]`; final
  raw=10/15, scaled=67%, band=credit, duration_ms=2,700,000.
- All four exit criteria covered:
  - 3-stage NAPLAN routes correctly per routing table ✅
  - Routing table honoured (low/high score paths) ✅
  - Stage boundaries enforced (canNavigateBack rules + cross-stage hard-block) ✅
  - Back-nav blocked across stages (Q-17.6) ✅
- Commit `3db1234` landed locally; **NOT pushed** in the implementation
  step — push deferred to evening ritual per the user's instruction.

## Outcome

- **Commit:** `3db1234` on `main` (local).
- **Tests added:** 33 (110 total in `@mm/engines`, was 77).
- **New ADR:** ADR-0024 (adaptive testlet routing model + seed correction).
- **Q-17.1 spec/seed mismatch resolved** in the same commit (seed rewrite +
  engine code + ADR atomic).
- **All 4 v1 engines complete:** Linear (§3.2.2), Skill (§3.2.3),
  Diagnostic (§3.2.4), Adaptive (§3.2.1). RepairEngine deferred to v1.1
  per BUILD_CONTRACT §2.
- **Forward-compatible state union** ready for Stage 17 was the union's
  fourth slot; v1.1's `RepairEngineState` is the next addition.
- **Stage 18 ready:** content-svc Edge Function will pre-resolve
  `EngineItem[]` (with `skill_ids`, `difficulty`, and now optional
  `testlet_id`/`stage_id` for adaptive sessions) and feed them to the
  engines via `SessionContext.planned_items`.
