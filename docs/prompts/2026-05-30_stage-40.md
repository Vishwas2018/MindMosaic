# Stage 40 C-C-D-V — Student Assignments + Dashboard v2

- Date: 2026-05-30
- Stage: 40 (Day 54, 3-day budget Days 54–56)
- T-discipline: T1 pre-reads complete; T2-tightened; T5 mockup-driven layout; pre-push verification MANDATORY (Stage 38/39 retro reinstatement). T1 read defect captured: explain-format.ts WAS present at packages/core/src/explain-format.ts (Stage 36 commit 56b4a9a) — do NOT re-create.

---

## Context

Stage 40 — Student Assignments page (Screen 13) + Student Dashboard v2 (Screen 7 upgrade).
All backend endpoints shipped Stages 31–34. Stage 40 is pure frontend + SDK hooks.

One SDK bug fix included: ISSUE-0026 (`useLearningPlan` path missing `${studentId}/current`). Fix is D1 and is committed first (T2-tightened sequence).

Authority documents (T1 pre-reads pinned):
- `SCREEN_SPECS.md §13` — student assignments page field names, states, tab labels (content authority; mockup governs layout)
- `SCREEN_SPECS.md §7` — student dashboard Week 40 additions (KPI tiles, plan widget, Bell, Quick Insights)
- `docs/mockups/10-student-assignments.html` — tab structure, card layout, visual tokens (T5 authority)
- `docs/mockups/02-dashboard.html` — dashboard v2 layout (T5 authority)
- `supabase/functions/assignments-svc/handlers.ts` — StudentAssignmentDTO, startAssignment return shape
- `supabase/functions/notifications-svc/handlers.ts` — NotificationDTO, GET /notifications/me
- `packages/types/src/orchestration.ts` — LearningPlanDTOSchema, LearningPlanItemDTOSchema

T1 DTO shapes (verbatim, pinned for stale-comment guard):

```typescript
// StudentAssignmentDTO (assignments-svc/handlers.ts, forStudent endpoint)
interface StudentAssignmentDTO {
  id: string;
  title: string;
  status: string;               // assignment status (published/archived)
  due_at: string | null;
  mode: string;
  item_count: number;
  auto_generated: boolean;
  created_at: string;
  published_at: string | null;
  archived_at: string | null;
  my_status: string | null;     // student's own status: 'pending' | 'in_progress' | 'completed'
  my_session_id: string | null; // session UUID if started or completed
  completed_at: string | null;
}

// NotificationDTO (notifications-svc/handlers.ts)
interface NotificationDTO {
  id: string;
  type: string;
  title: string;
  body: string;
  link: string | null;
  read: boolean;
  created_at: string;
}

// startAssignment return (assignments-svc/handlers.ts)
interface StartAssignmentResult {
  session_id: string;
  assignment_session_status: string;
}

// LearningPlanItemDTO (packages/types/src/orchestration.ts)
interface LearningPlanItemDTO {
  order: number;
  week: number;
  mode: string;
  target_skill_names: string[];
  target_skill_ids: string[];
  difficulty_label: string;
  estimated_duration_min: number;
  rationale: string;
  priority: string;
  status: string;
  // NOTE: NO id field — plan item navigation uses mode only (Q-40.3)
}

// LearningPlanDTO (packages/types/src/orchestration.ts)
interface LearningPlanDTO {
  plan_id: string;
  plan_type: string;
  status: string;
  created_at: string;
  valid_until: string;
  sessions: LearningPlanItemDTO[];
  milestones: unknown[];
  stale_since: string | null;
}
```

Assignments page tab structure (mockup authority, Q-40.2 resolution, DEV-20260530-1 filed):
- Tab 0: Assigned — all `my_status='pending'` cards, including overdue (red border variant)
- Tab 1: In Progress — `my_status='in_progress'` cards
- Tab 2: Completed — `my_status='completed'` cards
- Overdue banner above tabs when count > 0

Dashboard v2 additions (Screen 7, Stage 40):
- TopBar: Bell component (useMyNotifications unread count badge), 3-item nav (Dashboard / Assignments / Results)
- KPI tile grid (4 tiles): Sessions this week / Overall mastery / Weekly Progress (custom inline block, Q-40.UI-2) / Last Score
- Left 3/5 grid: Weekly Learning Plan card (useLearningPlan plan items) + Continue-last card (useListRecentSessions)
- Right 2/5 grid: Mastery Snapshot (SkillBars from useLearnerProfile) + Quick Insights (buildExplanationCards from @mm/core)
- Lower: Recent sessions table + Assessment shortcuts

---

## Constraints

C1. All assignments-svc and notifications-svc backend endpoints shipped Stages 31–34. No Deno/Edge Function changes in Stage 40.

C2. D1 (ISSUE-0026 fix) is committed before any hook or page code. useLearningPlan path:
    OLD: `.get('/orchestration-svc/orchestration/plan', LearningPlanDTOSchema)`
    NEW: `.get(\`/orchestration-svc/orchestration/plan/${encodeURIComponent(studentId)}/current\`, LearningPlanDTOSchema)`
    File: `packages/sdk/src/hooks/orchestration.ts` line 25.

C3. ADR-0029 SDK path prefix pattern: SDK paths = `/{svc-name}-svc/{internal-path}`.
    - notifications: `/notifications-svc/notifications/me?unread=true` (unreadOnly=true) or `/notifications-svc/notifications/me`
    - startAssignment: `POST /assignments-svc/assignments/{id}/start`
    - student assignments list: `/assignments-svc/assignments/for-student/{studentId}` (useStudentAssignments — ALREADY EXISTS Stage 38; do NOT recreate)

C4. useMyNotifications(unreadOnly?: boolean) — NEW hook in packages/sdk/src/hooks/notifications.ts.
    - mmKeys.notifications.all() and mmKeys.notifications.mine() added to keys.ts (D2).
    - QueryFn calls GET /notifications-svc/notifications/me (optionally appends ?unread=true).
    - Bell in dashboard uses useMyNotifications(true) — only unread count.

C5. useStartAssignment() — NEW mutation in packages/sdk/src/hooks/assignments.ts.
    - POST /assignments-svc/assignments/{id}/start, Idempotency-Key header required.
    - Returns StartAssignmentResult: { session_id: string; assignment_session_status: string }.
    - On success: navigate to /session/{session_id}/{mode} (use router.push; mode from StudentAssignmentDTO.mode).
    - Stale comment: "// ISSUE-0023 ongoing: Idempotency-Key accepted + logged server-side; dedup enforcement v1.1"

C6. MODE_ICON_MAP exported from apps/web/src/copy/student.ts (D7). Single consumer in v1.
    Map: { practice: PencilIcon, diagnostic: ClipboardListIcon, exam: ClockIcon }.
    Generic fallback: QuestionMarkCircleIcon.

C7. Plan item "Start" navigation (Q-40.3): `/session-selection?mode={item.mode}`. No plan_item_id in LearningPlanItemDTO; skill-param extension out of scope v1. Limitation comment at call site.

C8. Quick Insights (Q-40.4): `buildExplanationCards(useCausalMap(studentId).data?.active_misconceptions ?? [])` imported from `@mm/core`. Render first 3 cards. Do NOT re-create explain-format.ts — it exists at packages/core/src/explain-format.ts (Stage 36 commit 56b4a9a, 8 tests).

C9. Weekly Progress KPI tile: custom inline block (not StatTile — no children slot, Q-40.UI-2).

C10. TopBar nav = 3 items: Dashboard / Assignments / Results. `/learn` and `/practice` NOT added (no honest destination routes in v1, Q-40.UI-1).

C11. Review button on completed cards: plain button → `/results/{my_session_id}`. No dropdown (Q-40.UI-6, DEV-20260530-2 filed). my_session_id must be non-null for completed cards; null guard required (render "—" if null).

C12. Overdue detection: `due_at` is in the past AND `my_status='pending'`. "Overdue" pill + red `border-l-4 border-l-red-500`. Banner: "You have {N} overdue assignment{s}" above tab strip.

C13. Stale-comment guard. Do not reference Stage 37/38/39 variable names in new student files:
    - Stage 37: TeacherSidebarNav, ClassKpiStrip, InterventionAlertsSection, useTeacherRefresh
    - Stage 38: TeacherNotes, StrandMasteryCard, useFlagForReview, StudentAssignmentDTO (already in assignments.ts — may import but don't redeclare)
    - Stage 39: AssignmentWizard, DraftAssignmentDTO, useGenerateAssignment, WizardStepIndicator, toServerMode
    - No `any` in production code (TypeScript strict).

C14. PHASE-2 comment guard: any feature beyond v1 scope must carry `// PHASE-2:` comment and "Available in a future release" UI copy.

C15. Dashboard file target: `apps/web/src/app/(student)/dashboard/page.tsx` (DEV-20260511-2, Q-40.1). Root-level `(student)/page.tsx` is unreachable via middleware.

C16. Assignments page new file: `apps/web/src/app/(student)/assignments/page.tsx` (Screen 13). No existing file — create new.

C17. T5 mid-impl skeleton checkpoint MANDATORY: surface assignments page tab skeleton + dashboard TopBar/KPI skeleton to operator before data-wiring. Do not proceed to full data wiring until cleared.

C18. Pre-push verification MANDATORY (V1–V9). Surface all to architect before any git push.

---

## Deliverables

D1. `packages/sdk/src/hooks/orchestration.ts` (ISSUE-0026 fix, committed first)
    - Line 25: fix useLearningPlan queryFn path to include `${studentId}/current`.
    - +1 test in packages/sdk test suite: useLearningPlan fetches from `/orchestration-svc/orchestration/plan/${studentId}/current` (URL pattern match).

D2. `packages/sdk/src/keys.ts`
    - Add notifications namespace:
      · `notifications.all()` → `['notifications'] as const`
      · `notifications.mine()` → `['notifications', 'mine'] as const`

D3. `packages/sdk/src/hooks/notifications.ts` (new file)
    - `useMyNotifications(unreadOnly?: boolean)` — query, GET /notifications-svc/notifications/me
    - NotificationDTOSchema (Zod, mirrors NotificationDTO shape above)
    - queryKey: mmKeys.notifications.mine()
    - Append enabled guard: skip when not mounted server-side

D4. `packages/sdk/src/hooks/assignments.ts`
    - Add `useStartAssignment()` — mutation, POST /assignments-svc/assignments/{id}/start
    - StartAssignmentResultSchema: z.object({ session_id: z.string(), assignment_session_status: z.string() })
    - Send Idempotency-Key header (crypto.randomUUID() per call)
    - Export useStartAssignment from hooks/index.ts

D5. `apps/web/src/app/(student)/assignments/page.tsx` (NEW — Screen 13)
    - StudentTopBar: Bell + 3-item nav (Dashboard / Assignments[active] / Results)
    - Overdue banner (above tabs, conditional)
    - Tabs component (Radix-based, items: Assigned / In Progress / Completed with count pills)
    - AssignmentCard per tab:
      · Assigned (pending): mode icon + title + teacher/count/duration meta + due-date + [Start] button → useStartAssignment
      · Overdue variant: red border-l + "Overdue" pill + "Was due..." line + [Start] button
      · Due-soon variant (due ≤ 3 days): amber border + "Due Soon" pill + "{N} day(s) left"
      · In Progress: brand border + progress bar (no score yet) + [Continue] → /session/{my_session_id}/{mode}
      · Completed: green border + opacity-85 + score% + [Review] → /results/{my_session_id}
    - EmptyState per tab
    - LoadingState: skeleton cards
    - Data: useStudentAssignments(studentId) — EXISTING hook (Stage 38, hooks/assignments.ts); use useMe().data.id for studentId

D6. `apps/web/src/app/(student)/dashboard/page.tsx` (UPGRADE — Screen 7 Stage 40)
    - StudentTopBar: Bell (useMyNotifications(true) unread count) + 3-item nav (Dashboard[active] / Assignments / Results)
    - KPI tile grid (4 tiles):
      · Sessions: count from useListRecentSessions (last 7 days)
      · Mastery: overall from useLearnerProfile domain_profiles average
      · Weekly Progress: custom inline block — sessions this week / weekly plan count (or 5 default)
      · Last Score: useListRecentSessions.data[0]?.score_pct ?? '—'
    - Main grid (lg:grid-cols-5):
      · LEFT (col-span-3): Weekly Learning Plan card (useLearningPlan plan items, first 3 today's items, Start CTA per Q-40.3) + Continue-last card
      · RIGHT (col-span-2): Mastery Snapshot (SkillBars per domain_profiles strand, top 5) + Quick Insights (buildExplanationCards, first 3 cards)
    - Lower: Recent sessions table (useListRecentSessions, last 5) + Assessment shortcuts (3 tiles: Diagnostic / Practice / Mock Exam → /session-selection?mode=diagnostic etc.)
    - Preserve existing Greeting + PathwaySection + RecentSessionsSection; upgrade Mastery section to real SkillBars (closing ISSUE-0011f TODO comment)

D7. `apps/web/src/copy/student.ts` (new file)
    - MODE_ICON_MAP: Record<string, ComponentType> — practice/diagnostic/exam/skill_drill icons
    - Generic fallback icon for unmapped modes
    - Any other student-copy strings needed by D5/D6 (e.g., tab labels, empty state copy)

D8. Tests (+≥10 new):
    - packages/sdk: useLearningPlan URL fix — fetches correct path with studentId (1)
    - packages/sdk: useMyNotifications fetches /notifications-svc/notifications/me (1)
    - packages/sdk: useMyNotifications with unreadOnly=true appends ?unread=true (1)
    - packages/sdk: useStartAssignment POSTs to /assignments-svc/assignments/{id}/start (1)
    - packages/sdk: useStartAssignment sends Idempotency-Key header (1)
    - packages/sdk: mmKeys.notifications shapes (all/mine distinct, correct arrays) (1)
    - apps/web: assignments page renders Assigned tab with empty state (1, jest-axe or RTL)
    - apps/web: assignments page renders overdue banner when overdue items present (1)
    - apps/web: dashboard Bell renders unread count badge (1)
    - apps/web: dashboard Quick Insights renders ≤3 cards (1)

D9. `apps/web/playwright/e2e/student-assignments.spec.ts` (new, skip-guarded)
    - Guard: `test.skip(!process.env.E2E_WEB_URL, 'E2E_WEB_URL not set')`
    - Spec: assignments page loads 3 tabs; Assigned tab shows EmptyState or cards; Dashboard shows KPI grid.

---

## Verification

V1 — Test count: enumerate all new test names verbatim at close. Target: ≥591 total (581 baseline + ≥10 new).

V2 — T5 skeleton checkpoint: surface assignments page tab skeleton + dashboard KPI skeleton to operator BEFORE data wiring. MANDATORY pause point; do not proceed without clearance.

V3 — ISSUE-0026 fix grep:
    ```
    grep -n 'orchestration/plan' packages/sdk/src/hooks/orchestration.ts
    # → must contain ${studentId}/current in the path
    ```

V4 — useStartAssignment Idempotency-Key header:
    ```
    grep -n 'Idempotency-Key' packages/sdk/src/hooks/assignments.ts
    # → ≥3 hits (useCreateAssignment + usePublishAssignment from Stage 39 + useStartAssignment D4)
    ```

V5 — explain-format imported, not re-created:
    ```
    grep -rn 'buildExplanationCards' apps/web/src/app/\(student\)/dashboard/page.tsx
    # → import from '@mm/core'
    grep -rn 'buildExplanationCards' packages/core/src/explain-format.ts
    # → must show original file, not a duplicate
    ```

V6 — MODE_ICON_MAP present and exported:
    ```
    grep -n 'MODE_ICON_MAP' apps/web/src/copy/student.ts
    # → ≥1 hit (export)
    grep -n 'MODE_ICON_MAP' apps/web/src/app/\(student\)/assignments/page.tsx
    # → ≥1 hit (import)
    ```

V7 — Stale-comment guard (0 hits each):
    ```
    grep -nE 'TeacherSidebarNav|ClassKpiStrip|InterventionAlertsSection|useTeacherRefresh|TeacherNotes|StrandMasteryCard|useFlagForReview|AssignmentWizard|DraftAssignmentDTO|useGenerateAssignment|WizardStepIndicator|toServerMode' \
      apps/web/src/app/\(student\)/
    ```

V8 — /learn and /practice not in student nav:
    ```
    grep -rn "'/learn'\|'/practice'" apps/web/src/app/\(student\)/
    # → 0 hits in nav elements
    ```

V9 — no `any` in production code:
    ```
    grep -nE '\bany\b' packages/sdk/src/hooks/notifications.ts packages/sdk/src/hooks/assignments.ts apps/web/src/app/\(student\)/assignments/page.tsx apps/web/src/app/\(student\)/dashboard/page.tsx
    # → 0 hits in type positions
    ```

V10 — pnpm lint ✅, pnpm typecheck ✅, pnpm test ✅ (≥591), pnpm build ✅ — all gates green before push.

---

## T5 Layout Sketch

### Page 1 — /student/assignments (Screen 13)

TopBar (h-14 sticky bg-white border-b):
  Logo | Nav: [Dashboard] [Assignments*] [Results] | Bell(N) | Avatar

OverdueBanner (bg-red-50 border-l-4 border-l-red-500 p-3 mb-4, conditional):
  ⚠ "You have {N} overdue assignment{s} — please complete them as soon as possible."

TabStrip (Tabs component, border-b):
  [Assigned (N)] [In Progress (N)] [Completed (N)]

AssignmentCard variants (card p-4 border-l-4):

  Assigned (pending, not overdue):
    border-l-slate-200 | {ModeIcon} {title} | Teacher · {N} Qs · ~{min} min | Due {date}
    [Start] btn-primary (→ useStartAssignment → /session/{id}/{mode})

  Due Soon (due ≤ 3 days, pending):
    border-l-amber-400 | {ModeIcon} {title} | {N} day(s) left pill amber | ...
    [Start] btn-primary

  Overdue (past due_at, pending):
    border-l-red-500 | {ModeIcon} {title} | [Overdue] pill red | Was due {date}
    [Start] btn-primary

  In Progress:
    border-l-brand-500 | {ModeIcon} {title} | progress bar (indeterminate or session %) | ...
    [Continue] btn-secondary (→ /session/{my_session_id}/{mode})

  Completed:
    border-l-green-500 opacity-85 | {ModeIcon} {title} | {score}% | completed {date}
    [Review] btn-ghost (→ /results/{my_session_id})

EmptyState per tab: centered icon + copy (e.g., "No assignments yet — check back later")
LoadingState: skeleton cards (3 per tab)

---

### Page 2 — /dashboard (Screen 7 v2)

TopBar (h-14 sticky bg-white border-b):
  Logo | Nav: [Dashboard*] [Assignments] [Results] | Bell(N unread) | Avatar

Greeting (h2): "Good morning, {firstName}" + date

KPI tile grid (grid-cols-2 md:grid-cols-4 gap-4):
  [Sessions This Week  N sessions]
  [Overall Mastery     N%]
  [Weekly Progress     custom: {done}/{total} + progress bar]
  [Last Score          N% or —]

Main grid (lg:grid-cols-5 gap-6):

  LEFT (col-span-3):
    Weekly Learning Plan card (rounded-2xl bg-white shadow-sm p-5):
      h3 "This Week's Learning Plan" | stale chip if stale_since not null
      Plan items (max 3 today's): ModeIcon + mode label + skills chips + estimated_duration_min
      [Start] → /session-selection?mode={item.mode} (Q-40.3 limitation comment)
      "View full plan" link (stub → PHASE-2 placeholder)

    Continue Last card (rounded-2xl bg-brand-50 border border-brand-100 p-5):
      Last session subject + mode + CTA [Continue] → /session/{id}/{mode}
      (hidden if no recent sessions)

  RIGHT (col-span-2):
    Mastery Snapshot card:
      h3 "Mastery Snapshot"
      SkillBars (layout="horizontal") per domain_profiles strand — top 5 by mastery desc

    Quick Insights card:
      h3 "Quick Insights"
      3 rows: ExplanationCard {icon} {title} · {body} (buildExplanationCards from @mm/core)
      (hidden if no active_misconceptions)

Lower:
  Recent Sessions table (useListRecentSessions, last 5): Date | Subject | Score | Mode | [Review]
  Assessment shortcuts (3 tiles): Diagnostic / Practice / Mock Exam → /session-selection?mode=...

---

## Q resolutions governing Stage 40

Q-40.1 (T2): Dashboard file = dashboard/page.tsx; DEV-20260511-2 filed.
Q-40.2 (T5): 3 tabs Assigned/In Progress/Completed; Overdue in Assigned; DEV-20260530-1 filed.
Q-40.3 (T2): Plan item Start → /session-selection?mode={item.mode}; no id field on LearningPlanItemDTO.
Q-40.4 (T2): Quick Insights = buildExplanationCards from @mm/core (confirmed present Stage 36).
Q-40.UI-1 (T5): 3-item TopBar nav (Dashboard / Assignments / Results); no /learn or /practice.
Q-40.UI-2 (T5): Weekly Progress = custom inline block; not StatTile extension.
Q-40.UI-5 (T2): MODE_ICON_MAP in apps/web/src/copy/student.ts (practice/diagnostic/exam/skill_drill).
Q-40.UI-6 (T2): Completed card = plain Review button → /results/{my_session_id}; DEV-20260530-2 filed.

---

## Deviations applicable to Stage 40

- DEV-20260511-2: Stage 40 DEV_PLAN path typo (dashboard/page.tsx, not page.tsx) — filed this prep commit.
- DEV-20260530-1: Tab label deviation (Assigned/In Progress/Completed vs SCREEN_SPECS To do/Completed/Overdue) — filed this prep commit.
- DEV-20260530-2: Review button vs "View history dropdown" (SCREEN_SPECS §13) — filed this prep commit.
- ISSUE-0026: useLearningPlan path bug — closed by D1.
- DEV-20260523-1: Idempotency-Key server enforcement deferred; client sends header for startAssignment (D4).

---

## T1 Read Defect Retro Note (captured at Stage 40 session start)

Morning ritual R7 claimed `packages/core/src/explain-format.ts` "does not exist in the codebase." The file DOES exist — Stage 36 commit `56b4a9a` shipped `buildExplanationCards()` v1 + 8 tests. Search returned no results because the search target was `apps/web/src` (scoped to the web app) rather than `packages/core/src`. Verified via `ls packages/core/src/explain-format.ts` at Stage 40 session start — confirmed present. Do NOT re-create. Import via `@mm/core`. Retro note to be included in Stage 40 DAILY_LOG close.
