# Stage 8 C-C-D-V Prompt — 2026-05-03

## Context

Stage 8 of 75: Migration 0007 — New Domains (Assignments + Billing + Engagement + Notifications).
11 new tables from arch §2.11–§2.14. New SECURITY DEFINER helper fn_my_assignment_ids().
FK from session_record.assignment_id to assignment (noted in 0004 header, deferred to Stage 8).

Preceding state: 334/334 pgTAP green (42 tables). All quality gates green.

§2A pre-implementation review completed. Key decisions:
- Q1: notification_preference — not in Stage 8 (v1.1 feature). Excluded.
- Q2: weekly_goal — not a v1 table. Excluded.
- D-A REVERSAL: engagement (engagement_streak, achievement_definition, student_achievement)
  Pattern G (not Pattern A); engagement worker is Stage 28+ service, no v1 writer.
- D-B: assignment + assignment_session state transitions enforced at service layer (ADR-0016);
  no DB state-machine triggers.
- D-C: billing tables all Pattern G uniform (subscription, billing_customer, invoice,
  billing_event); Stripe activation deferred to Stage 42.
- D-D corrected to D-D': notification FOR ALL per arch Pattern E (not FOR SELECT); PATCH
  /notifications/{id}/read uses Bearer auth JWT → needs UPDATE policy.

Amendments applied before code:
- A1: triple REVOKE corrected to PUBLIC + authenticated + anon (not PUBLIC×2). BUILD_CONTRACT §6
  and PGTAP_PATTERNS P3 updated. fn_my_assignment_ids() uses correct pattern.
- A2: removed redundant `AND at2.class_id IS NOT NULL` from UNION second branch of
  fn_my_assignment_ids() (unreachable — JOIN on class_id NOT NULL FK guarantees non-null).
- A3: added G11.7 WITH CHECK enforcement test (INSERT with user_id=user_B under user_A JWT
  → throws '%row-level security%'). plan 71→72.
- V1: PATCH /notifications/{id}/read confirmed Bearer auth → FOR ALL correct.
- V2: git status confirmed no orphaned ADR files after Stage 7 H1 git clean.

## Constraints

- Pattern A: assignment (student via fn_my_assignment_ids(); staff+padmin by tenant).
- Pattern A: assignment_session (student own; staff+padmin by tenant).
- Pattern G: assignment_target, subscription, billing_customer, invoice, billing_event,
  engagement_streak, achievement_definition, student_achievement (ADR-0015).
- Pattern E: notification (FOR ALL user_id = auth_user_id(); WITH CHECK).
- ADR-0014: structural pg_indexes catalog + P5 dedup; no EXPLAIN.
- ADR-0016: no DB state-transition triggers.
- A1: triple REVOKE: PUBLIC + authenticated + anon.
- LANGUAGE sql functions must be created AFTER all referenced tables in same migration.
- pg_class relnamespace filter required for tables that conflict with Supabase system schemas.
- DML CTE must be top-level statement, not inside a subquery expression.
- session_record.assignment_id FK: constraint name fk_session_assignment (arch §2.11).
- XOR CHECK on assignment_target: (student_id IS NOT NULL) <> (class_id IS NOT NULL).
- idx_sub_active_per_tenant: partial UNIQUE (tenant_id) WHERE is_active = true (IMMUTABLE bool).

## Deliverables

1. `supabase/migrations/0007_new_domains.sql` — 11 tables, fn_my_assignment_ids(), FK,
   11 indexes, 5 updated_at triggers, full RLS.
2. `supabase/migrations/down/0007_new_domains.down.sql` — DROP in reverse FK order.
3. `supabase/tests/rls/007_new_domains.sql` — plan(72), 406/406 cumulative.
4. `docs/dev/decisions/0015-pattern-g-for-no-v1-writer-tables.md` — ADR-0015.
5. `docs/dev/decisions/0016-service-owned-state-machine.md` — ADR-0016.
6. `BUILD_CONTRACT.md` §6 + `PGTAP_PATTERNS.md` P3: A1 triple REVOKE correction.

## Verification

```
pnpm test:migration   # roundtrip up→down→up
pnpm test:rls         # 406/406 cumulative (334 + 72 Stage 8 tests)
pnpm turbo typecheck lint test  # 18 packages green (all cached)
```

All verification passed.

## Surprises / scope adjustments

- fn_my_assignment_ids() must be created after assignment_target (LANGUAGE sql validates body
  at CREATE time). Moved from Section 1 to Section 2 (after tables).
- realtime.subscription conflict: pg_class WHERE relname = 'subscription' returns two rows.
  Fixed with relnamespace = 'public'::regnamespace filter. G_meta.2 fixed with schemaname = 'public'.
- DML CTE restriction: G11.5 (silent UPDATE deny) initially used nested CTE inside SELECT is();
  refactored to top-level WITH _upd AS (...) SELECT is(...).
- BUILD_CONTRACT §6 A1 correction applied: "PUBLIC×2 + anon" updated to "PUBLIC + authenticated + anon".
