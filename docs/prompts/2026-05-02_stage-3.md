# Stage 3 C-C-D-V Prompt — 2026-05-02

## Context

Stage 3 — Migration 0002 — Content & Skill Graph.

Prior state: Migration 0001 (enums + tenancy + auth) complete. pgTAP 65/65 pass.
§2A pre-implementation review completed (ADR-0007, ADR-0008 filed).
C-C-D-V approved after 13-item correction round + 2 final verification questions.

## Constraints

- BUILD_CONTRACT §6: RLS in same migration as CREATE TABLE. No follow-up migration.
- Pattern F: ENABLE ROW LEVEL SECURITY (no FORCE). Service_role bypasses.
- Draft graph isolation: fn_graph_version_is_published() SECURITY DEFINER per ADR-0005.
- publish_skill_graph SECURITY DEFINER: reads draft edges (hidden to authenticated) + writes sgv.
- G4 guard: to_regclass + EXECUTE (not plain SELECT) — PL/pgSQL parses full SQL at execution time.
- Cycle detection ordering: cycle FIRST, G4 guard SECOND (ADR-0007).
- v_item_current WITH (security_invoker = true) — PG 15+ requirement.
- item_version: at most one is_current per item enforced by partial unique index.
- pgTAP plan(40): use throws_like for message checks; throws_ok 3-arg = (sql, errcode, errmsg);
  use has_function_privilege() for no-execute-grant assertions (avoids NULL-arg crash).

## Deliverables

1. `supabase/migrations/0002_content_skill_graph.sql`
   - SECTION 1: 9 tables (sgv, skill_node, skill_edge, skill_migration_map, misconception,
     repair_sequence, stimulus, item, item_version) + all indexes + constraints
   - SECTION 2: v_item_current WITH (security_invoker = true)
   - SECTION 3: fn_graph_version_is_published SECURITY DEFINER helper
   - SECTION 4: 5 set_updated_at triggers (sgv, skill_node, misconception, repair_sequence, item)
   - SECTION 5: publish_skill_graph SECURITY DEFINER plpgsql function with:
     - STEP 1: prerequisite cycle detection (slug-path RAISE via unnest+ordinality+LEFT JOIN)
     - STEP 2: G4 guard via IF+EXECUTE blocks (to_regclass forward-compatible)
     - STEP 3: archive current published version
     - STEP 4: update counts + publish; IF NOT FOUND single-message RAISE
     - REVOKE FROM PUBLIC + REVOKE FROM authenticated (double revoke for env defaults)
   - SECTION 6: Pattern F RLS on all 9 tables

2. `supabase/migrations/down/0002_content_skill_graph.down.sql`
   - DROP VIEW → DROP FUNCTION publish_skill_graph → DROP TABLE (9, reverse FK) → DROP FUNCTION helper

3. `supabase/tests/rls/002_content.sql`
   - plan(40), 12 groups: G1 RLS enabled (9), G2 fn shape (4), G3 publish_fn shape (4),
     G4 triggers (5), G5 v_item_current (2), G6 cycles (4), G7 forked DAG (1),
     G8 isolation pre (3), G9 clean publish (2), G10 isolation post (3),
     G11 G4 guard stub (2), G12 permission check (1)

## Verification

- `pnpm turbo typecheck lint test` → 18/18 (all cached) ✅
- `npx supabase test db supabase/tests/rls` → 105/105 (001: 65/65, 002: 40/40) ✅
- Migration roundtrip via docker exec psql: up→down→verify clean→up ✅

## Actual issues encountered

1. PL/pgSQL parse-time table resolution (ADR-0007 correction): plain SELECT short-circuit
   insufficient; EXECUTE required. 3 failures → fix: IF+EXECUTE pattern.
2. throws_ok 3-arg signature: arg3 = errmsg, not description. 2 failures → fix: throws_like
   for message patterns; separate description via 4-arg form.
3. 4-arg throws_ok with NULL errmsg crashes server → fix: use has_function_privilege() catalog
   check for G12 permission-denied assertion.
4. Supabase default EXECUTE grant to authenticated: REVOKE FROM PUBLIC insufficient → fix:
   double REVOKE (PUBLIC + authenticated).
5. supabase db reset container restart timeouts on Windows/Docker Desktop: cosmetic, resolved
   via docker exec psql manual roundtrip.
