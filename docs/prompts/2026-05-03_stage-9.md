# Stage 9 C-C-D-V Prompt — 2026-05-03

## Context

Stage 9 of 75: Migration 0008 — pg_cron Setup. 8 SQL functions (LANGUAGE sql VOLATILE; no
SECURITY DEFINER) + 8 cron job registrations. No new tables; no RLS. fn_recalibrate_content()
is a PHASE-2 no-op stub per arch Part XI. cron.schedule() idempotency pattern used (iv REVERSED
from DEV_PLAN.md "ON CONFLICT DO NOTHING" documentation imprecision — ADR-0017).

Preceding state: 428/428 pgTAP after Stage 8 (406 prior + 22 Stage 9). All quality gates green.

§2A pre-implementation review completed. Key decisions:
- (i) APPROVED Option 2: content.recalibration as no-op stub per arch Part XI; PHASE-2 comment.
  DEVIATION DEV-20260503-2.
- (ii) APPROVED: abandoned.cleanup threshold = 24 hours; comment "tunable in future migration".
- (iii) APPROVED: jobs.archive semantics = DELETE (no archive table).
- (iv) REVERSED: use cron.schedule() / cron.unschedule() public API, not direct INSERT into
  cron.job. ADR-0017 filed.
- (v) CONFIRMED: No SECURITY DEFINER; LANGUAGE sql VOLATILE; no REVOKE/GRANT.

Amendments applied before code:
- A1: fn_recalibrate_content() body uses `UPDATE job_queue SET status = status WHERE FALSE`
  (valid LANGUAGE sql RETURNS void no-op; SELECT 1 rejected at CREATE time — int not castable
  to void).
- A2: Down migration does NOT drop pg_cron extension — Supabase pre-loads it; IF NOT EXISTS
  in up migration is idempotent.
- ADR-0017 filed at evening ritual.
- DEV-20260503-2 filed at evening ritual.

## Constraints

- LANGUAGE sql VOLATILE for all 8 cron functions; no SECURITY DEFINER; no REVOKE/GRANT.
- cron.schedule() / cron.unschedule() public API (not direct INSERT into cron.job) — ADR-0017.
- Unschedule-first idempotency: SELECT cron.unschedule(jobid) FROM cron.job WHERE jobname='<n>';
  SELECT cron.schedule(...).
- content.recalibration: no-op body + PHASE-2 comment per arch Part XI.
- abandoned.cleanup threshold: 24 hours; comment "tunable in future migration".
- jobs.archive: DELETE (no archive table).
- LANGUAGE sql bodies validated at CREATE time; all referenced tables exist in 0001–0007.
- No DROP EXTENSION in down migration.
- Test UUID namespace: 0009-xxxx.
- plan(22): G_ext(1) + G_cron(8) + G_fn(8) + G_behavioral(5).

## Deliverables

1. `supabase/migrations/0008_cron.sql` — CREATE EXTENSION IF NOT EXISTS pg_cron, 8 functions,
   8 cron registrations via unschedule-first + cron.schedule()
2. `supabase/migrations/down/0008_cron.down.sql` — unschedule×8 + DROP FUNCTION×8
3. `supabase/tests/rls/008_cron.sql` — plan(22), 428/428 cumulative
4. `docs/dev/decisions/0017-cron-schedule-api-not-direct-insert.md` — ADR-0017
5. DEVIATION DEV-20260503-2 appended to DEVIATIONS.md

## Verification

```
pnpm test:migration   # roundtrip up→down→up — PASSED
pnpm test:rls         # 428/428 cumulative (406 + 22 Stage 9) — PASSED
pnpm turbo typecheck lint test  # 18 packages green (all cached) — PASSED
```

All verification passed.

## Surprises / scope adjustments

- fn_recalibrate_content no-op body: `SELECT 1` rejected at CREATE time for RETURNS void
  (integer not castable to void). Fixed with DML no-op:
  `UPDATE job_queue SET status = status WHERE FALSE`.
- Down migration extension handling: not dropping pg_cron avoids destroying Supabase
  pre-loaded state. `CREATE EXTENSION IF NOT EXISTS` in up migration is idempotent on re-run.
- cron.schedule() two-statement pattern (unschedule + schedule) is slightly more verbose than
  DEV_PLAN.md's single-INSERT approach but is the version-stable documented API (ADR-0017).
