# Stage 4 C-C-D-V Prompt — 2026-05-02

## Context

Stage 4 — Migration 0003 — Assessment Configuration.

Prior state: Migration 0002 (Content & Skill Graph) complete. pgTAP 105/105 pass.
§2A pre-implementation review completed (2-cycle: initial review + 6 corrections applied).
ADR-0009 filed during §2A. C-C-D-V approved after VERIFICATION 1 (auth_role() claims path)
and VERIFICATION 2 (pathway.required_feature_key convention deferred to Stage 14).

## Constraints

- BUILD_CONTRACT §6: RLS in same migration as CREATE TABLE.
- Pattern F + ADR-0009: platform_admin write-only (not org_admin) for all 5 tables.
- auth_role() reads request.jwt.claims -> 'app_metadata' ->> 'role' (verified VERIFICATION 1).
- framework_config.blueprint jsonb AND blueprint table both exist verbatim (arch §2.4).
- pathway, assessment_profile: SELECT USING (is_active = true).
- framework_config, blueprint, diagnostic_rule: SELECT USING (true).
- No FORCE ROW LEVEL SECURITY (service_role bypasses per Pattern F).
- No SECURITY DEFINER helpers (no cross-table predicates in Stage 4 tables).
- pgTAP JWT claims role simulation: new pattern — skeleton required per ADR-0006.
- pathway.required_feature_key convention deferred to Stage 14 (no CHECK constraint).

## Deliverables

1. `supabase/migrations/0003_assessment_config.sql`
   - SECTION 1: 5 tables (framework_config, blueprint, pathway, assessment_profile,
     diagnostic_rule) + UNIQUE idx_fc_family_version + btree idx_pathway_feature_key
   - SECTION 2: Pattern F RLS (ADR-0009 platform_admin write only) on all 5 tables;
     is_active SELECT filter on pathway + assessment_profile

2. `supabase/migrations/down/0003_assessment_config.down.sql`
   - DROP TABLE 5 tables in reverse FK order: diagnostic_rule → assessment_profile →
     pathway → blueprint → framework_config

3. `supabase/tests/rls/003_assessment_config.sql`
   - plan(40), 8 groups: G1 RLS enabled (5), G2 key columns (15), G3 indexes (2),
     G4 non-admin INSERT rejected (5), G5 platform_admin INSERT succeeds (5),
     G6 SELECT active rows visible (5), G7 inactive rows hidden (2), G8 CHECK constraint (1)

## Verification

- `pnpm turbo typecheck lint test` → 18/18 (all cached) ✅
- `npx supabase test db supabase/tests/rls` → 145/145 (001: 65/65, 002: 40/40, 003: 40/40) ✅
- Migration roundtrip via docker exec psql: up→down→verify clean→up ✅

## Actual issues encountered

1. §2A Substantive 1 (pre-code): arch §3.2 Pattern F template listed both org_admin and
   platform_admin as write roles. Corrected during §2A: assessment config is platform-level
   catalog (not tenant-scoped); org_admin write would corrupt content for all tenants. ADR-0009
   filed capturing precedent + table-classification heuristic for Stages 5–10.

2. §2A Substantive 2 (pre-code): JWT claims role simulation required new pgTAP pattern.
   auth_role() reads nested path (app_metadata.role, not top-level role). Verified against
   Migration 0001 source (VERIFICATION 1). Skeleton documented in §2A and PROJECT_STATE.

3. §2A Correction 2 (pre-code): framework_config.blueprint jsonb column naming collision
   with blueprint table. Both exist verbatim per arch §2.4 — case (a), different purposes.
   Comment added in migration to clarify embedded template vs profile instance distinction.

4. All three issues resolved during §2A review — no code-phase debugging required.
   First stage in this build with zero post-coding failures.
