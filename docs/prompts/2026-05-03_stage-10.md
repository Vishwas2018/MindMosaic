# Stage 10 C-C-D-V Prompt — Outbox Dispatcher
# Date: 2026-05-03

## Context

Stage 10 Outbox Dispatcher. `outbox_event` (Migration 0006 / 0004) → `job_queue` (Migration 0006).
pg_cron available (Migration 0008). A1 security pattern established (BUILD_CONTRACT §6).
No SECURITY DEFINER on cron functions (ADR-0017). ADR-0018 to be filed this stage.
Six pre-cues answered before implementation:
  (i) APPROVED Option A: every-minute cron, ADR-0018, v1.1 = Database Webhook rewrite.
  (ii) APPROVED: fn_drain_outbox_batch RETURNS int (not void) for observability.
  (iii) APPROVED: job_queue only; forward-flag Stage 15+ pipeline_event in PROJECT_STATE.
  (iv) CONFIRMED: idempotency_key = 'outbox:' || event.id::text.
  (v) APPROVED Option A with ISSUE-0004: outbox_event 7-day cleanup, low, Stage 14 deadline.
  (vi) NEW: unknown event_type → RAISE EXCEPTION P0001 (fail loud, transaction rollback).
Three execution directives:
  X1: triple REVOKE (PUBLIC/authenticated/anon) + verify via proacl; GRANT TO service_role.
  X2: plan(10)→plan(11) — add G_behavioral.6b no-duplicate-jobs assertion after second drain.
  X3: Edge Function returns { drained: int, took_ms: int }, Content-Type: application/json.

## Constraints

- fn_drain_outbox_batch: LANGUAGE plpgsql VOLATILE, RETURNS int, no SECURITY DEFINER
- SET search_path = public, pg_temp
- FOR UPDATE SKIP LOCKED drain loop; RAISE EXCEPTION on unknown event_type
- idempotency_key = 'outbox:' || event.id::text; ON CONFLICT DO NOTHING
- Cron: every minute (ADR-0018), unschedule-first registration (ADR-0017)
- Down migration: unschedule + DROP FUNCTION (no extension drop)
- pgTAP: plan(11), new file 010_outbox_dispatcher.sql, UUID namespace 0010-xxxx, cumulative 451
- ISSUE-0004 filed in OPEN_ISSUES.md; ADR-0018 in docs/dev/decisions/

## Deliverables

1. supabase/migrations/0010_outbox_dispatcher.sql
2. supabase/migrations/down/0010_outbox_dispatcher.down.sql
3. supabase/functions/outbox-dispatcher/index.ts
4. supabase/tests/rls/010_outbox_dispatcher.sql (plan 11, cumulative 451)
5. docs/dev/decisions/0018-outbox-cron-every-minute.md (ADR-0018)
6. docs/dev/OPEN_ISSUES.md updated (ISSUE-0004)

## Verification

- pnpm test:migration — roundtrip up→down→up clean (all 10 migrations)
- pnpm test:rls — 451/451
- pnpm lint && pnpm typecheck — green
- X1 proacl check: "postgres=X/postgres, service_role=X/postgres" — no PUBLIC/anon/authenticated

## Outcome

All deliverables produced. All verification gates passed.
X1: Supabase did NOT auto-grant on LANGUAGE plpgsql (non-SECURITY DEFINER). REVOKE idempotent.
    proacl = "postgres=X/postgres, service_role=X/postgres". Clean.
pgTAP: 451/451. Roundtrip: clean. Lint/typecheck: cached green.
