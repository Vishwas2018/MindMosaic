# Stage 25 — C-C-D-V Prompt
# Student Dashboard v1 (Day 33, 1-day budget)
# Executed: 2026-05-15

---

## Context

Replace `apps/web/src/app/(student)/dashboard/page.tsx` (current EmptyState stub)
with the v1 Student Dashboard. This is the last Phase 1 UI screen.

**Route:** `/dashboard` — confirmed load-bearing via middleware +
`apps/web/src/lib/auth/role-home.ts`. DEV_PLAN's stated `(student)/page.tsx` is
wrong (DEV-20260515-1). Do NOT create a root-level page.tsx.

**SDK hooks available in v1:**
- `useMe()` — `UserMeDTO { display_name, year_level, … }`
- `useListRecentSessions()` — `SessionSummaryDTO[]` ordered `submitted_at DESC`
  (PostgreSQL NULLS FIRST = active sessions at index 0)
- `usePathways()` — `PathwayDTO[] { entitled, locked_reason, … }`

**SDK hooks NOT available in v1 (Stage 28+):**
- `useLearningDNA`, `useSkillProgress`, `useCausalMap` — all gated Stage 28+
- `useLearnerProfile` — does not exist yet

**Known v1 DTO gaps (from Q-25.1..4 resolutions):**
- `SessionSummaryDTO.pathway_name` is always null — show `mode` label instead
- No streak DTO — stub "—" with "Coming soon" micro-text
- No mastery percentage — show `skills_touched_count` aggregate (`StatTile` only;
  no ProgressBar — a 0% bar is visually misleading per Q-25.2)
- `submitted_at === null` at index 0 = active session → "Continue" CTA
- Locked pathway tiles: show grayed + lock icon + `locked_reason` (or "Upgrade to
  access") — do NOT hide (Q-25.4)

**`@mm/ui` primitives available:** `AppShell (student-parent)`, `TopBar`, `Brand`,
`Button`, `Card`, `EmptyState`, `StatTile`, `ProgressBar` (not used for mastery
stub), `Badge`.

**ADR-0029:** SDK hooks prepend service prefix in path
(e.g. `/assessment-svc/sessions/recent`). `MmClient` base =
`${NEXT_PUBLIC_SUPABASE_URL}/functions/v1`.

**Shell variant:** `AppShell variant="student-parent"` + `TopBar` with `Brand`.
No `FocusHeader` (Dashboard is not a focus-mode screen).

**`'use client'`** required (three hooks at page level).

**Open issues informational only (no action):**
- ISSUE-0007 / ISSUE-0008: no write mutations on Dashboard
- ISSUE-0005: `.env.local.example` — stays deferred to audit pass

---

## Constraints

- Six sections in strict order per SCREEN_SPECS Screen 7:
  1. Greeting card
  2. Continue-last / Start-first-session CTA
  3. Quick-start pathway tiles
  4. Mastery snapshot (StatTile stub — ISSUE-0011(f))
  5. Recent sessions table (last 5; mode label not pathway_name)
  6. Engagement strip (sessions-this-week real; streak stub)

- **NO Weekly Plan widget** — Stage 40.
- No `any` in production code. TypeScript strict throughout.
- `tokens.css` wins all style divergences.
- Locked pathway tiles: grayed + lock icon + locked_reason (never hidden).
- Mastery stub: `StatTile` "Skills touched: {N}" + "Full mastery data in a future
  release" micro-text. No ProgressBar at 0%.
- Streak stub: "—" + "Coming soon" micro-text.
- Sessions-this-week: computed client-side (filter `submitted_at` within current
  ISO week, i.e. `new Date(s.submitted_at) >= startOfCurrentISOWeek()`).
- Recent sessions empty state: `EmptyState` with "No sessions yet" + "Start your
  first session" CTA.
- Conventional commit: `feat(web): Stage 25 — student dashboard v1`

**Side-task candidate (opportunistic):** ISSUE-0012 `commit-msg` hook (low severity).
Include only if dashboard closes with budget to spare. Never risk main scope.

---

## Deliverables

1. **`apps/web/src/app/(student)/dashboard/page.tsx`** — full implementation:
   `'use client'`; `useMe`, `useListRecentSessions`, `usePathways`; six sections
   in order; loading skeleton; error states.

2. **Tests** — add to `packages/ui/src/components/__tests__/` if new `@mm/ui`
   components are introduced; otherwise add to
   `apps/web/src/__tests__/dashboard.test.tsx`:
   - 1 axe scan (no violations)
   - Continue-last CTA renders when `sessions[0].submitted_at === null`
   - "Start first session" CTA renders when sessions array is empty
   - Mastery stub renders with aggregated `skills_touched_count`
   - Locked pathway tile renders grayed (not hidden)

3. **`apps/web/playwright/e2e/dashboard-flow.spec.ts`** — `test.skip()`-guarded
   happy path (login → `/dashboard` → verify 6 sections present + Continue CTA
   when active session exists). Requires `E2E_BASE_URL` + `E2E_WEB_URL` +
   `E2E_SUPABASE_ANON`.

4. **Evening ritual** — DAILY_LOG append; PROJECT_STATE overwrite; full
   OPEN_ISSUES.md audit pass (ISSUE-0005..0012 all triaged; ISSUE-0013 filed for
   learner-profile upgrade); QUESTIONS.md review; DEVIATIONS.md check.

---

## Verification

- `pnpm lint` ✅ (no new warnings)
- `pnpm typecheck` ✅ (10 packages clean)
- `pnpm test` ✅ (unit count ≥ 387 = 383 + 4 new dashboard tests)
- `pnpm build` ✅ (`/dashboard` route builds clean; First Load JS within budget)
- axe scan passes — 0 violations on dashboard page
- Continue-last CTA renders when `sessions[0].submitted_at === null`
- EmptyState renders when `sessions` is empty
- Mastery stub (`StatTile` "Skills touched: N") renders with skills_touched_count > 0
- Locked pathway tile is visible and grayed (not absent from DOM)
- Streak shows "—" with stub copy
- DEV-20260515-1 deviation logged ✅
- ISSUE-0011(f) appended ✅
- Q-25.1..4 resolved ✅
- Audit day items completed at stage close ✅

---

## Q-25.* resolutions (baked in)

| Q     | Resolution |
| ----- | ---------- |
| Q-25.1 | Replace `dashboard/page.tsx` (not root `page.tsx`). DEV-20260515-1 filed. |
| Q-25.2 | `StatTile` with `skills_touched_count` aggregate. No ProgressBar. ISSUE-0011(f) filed. |
| Q-25.3 | Engagement strip renders. Streak = "—" stub. Sessions-this-week = client-side filter. |
| Q-25.4 | Locked tiles: show grayed + lock icon + `locked_reason` (never hidden). |
