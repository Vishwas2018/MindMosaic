# Stage 11 C-C-D-V Prompt — packages/types + Zod Schemas
# Date: 2026-05-03

## Context

Stage 11: packages/types — source-of-truth DTOs for client + server. Zod 3.x (installed this
stage: zod@3.25.76). First TypeScript-heavy stage; no pgTAP. Consumed by every service.
Four pre-cues answered before implementation:
  Q1: APPROVED — zod@^3.23 as production dependency of @mm/types; pnpm add --filter @mm/types.
  Q2: APPROVED with tightening — ProficiencyMapDTO uses 4-band enum (novice/developing/proficient/
      mastered). Confirmed distinct from SkillProgressDTO.status (5-band, §6.4). No conflict.
  Q3: APPROVED — branded ID set per arch §6 + 2 additions: GraphVersionId, FrameworkConfigId.
  Q4: APPROVED — Zod-first pattern: z.infer<> only, no parallel interface declarations.
Four execution directives:
  X1: DB enum parity test — hardcoded DB_ENUM_VALUES citing Migration 0001 line numbers.
      16 enums tested in schemas.test.ts.
  X2: Unique symbol brand pattern — `declare const Brand: unique symbol; type T = string & { [Brand]: never }`.
  X3: Exhaustive schema registry — filter exports ending in 'Schema', assert instanceof ZodType.
  X4: SCHEMA_VERSION = '1.0.0' as const with semantics comment in shared.ts.

## Constraints

- Zod-first: schemas defined first, types derived via z.infer<>. No parallel interface declarations.
- Unique symbol brand pattern for all ID types (X2). No `_brand` string approach.
- NodeNext module resolution: .js extensions on all relative imports in index.ts and cross-file.
- DB enum values must match 0001_enums_tenancy_auth.sql exactly — no invention.
- Zero `any` in production code (TypeScript strict).
- SCHEMA_VERSION '1.0.0' with X4 comment (Stage 12 SDK reads for version negotiation).
- Import graph must be a DAG — no circular dependencies.
- ErrorCode: 15 codes per arch §1.5, not a DB enum, no X1 parity test.
- ProficiencyMapDTO 4-band MasteryBand: distinct from SkillProgressDTO.status (5-band §6.4).
- X3: exhaustive registry test asserts ≥30 ZodType exports from index.

## Deliverables

1. packages/types/src/shared.ts — SCHEMA_VERSION, 10 branded IDs, 16 DB enums, ErrorCode, APIErrorEnvelope
2. packages/types/src/identity.ts — UserMeDTO, TenantDTO
3. packages/types/src/content.ts — PathwayDTO, AssessmentProfileDTO, ItemDTO
4. packages/types/src/session.ts — 8 schemas (CreateSession*, RecordResponse*, SubmitSession, SessionState, SessionSummary, Checkpoint)
5. packages/types/src/intelligence.ts — BehaviourProfile, SkillProgress, RepairSession, CausalMap, ExplanationDTO, LearningDNA
6. packages/types/src/orchestration.ts — LearningPlanItem, LearningPlan, PathwayReadiness, PlanOverrideRequest
7. packages/types/src/assignments.ts — AssignmentDTO, CreateAssignmentRequest, StudentAssignmentDTO, AssignmentTrackingDTO
8. packages/types/src/analytics.ts — InterventionAlert, CohortOverview, AutoGroupDTO
9. packages/types/src/billing.ts — PlanCatalog, Subscription, Checkout*, InvoiceDTO
10. packages/types/src/engagement.ts — EngagementSummary, Achievement, NotificationDTO
11. packages/types/src/admin.ts — JobStatusDTO, PipelineEventDTO
12. packages/types/src/proficiency.ts — MasteryBandSchema, ProficiencyMapDTO
13. packages/types/src/index.ts — re-exports all with .js extensions
14. packages/types/src/__tests__/schemas.test.ts — 97 tests (X1, X3, parse smoke tests)

## Verification

- pnpm --filter @mm/types typecheck — green
- pnpm --filter @mm/types lint — green
- pnpm --filter @mm/types test — 97/97
- pnpm typecheck && pnpm lint && pnpm test — 6/6 green monorepo-wide
- Zero `any` in packages/types/src/*.ts (grep clean)
- `import { CreateSessionResponseSchema } from '@mm/types'` resolves (typecheck confirms)

## Outcome

All deliverables produced. All verification gates passed.
Typecheck: 6/6 green. Lint: 6/6 green. Tests: 97/97 (unit, first real test count in project).
Zero `any`. No circular deps. Import graph DAG confirmed.
zod@3.25.76 installed (was missing before this stage).
ProficiencyMapDTO 4-band confirmed distinct from SkillProgressDTO.status 5-band (no conflict).
No ADR-0019 needed — all X1–X4 patterns followed pre-approved plan.
Commit: 6536bdc
